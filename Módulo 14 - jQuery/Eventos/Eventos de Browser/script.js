$(function() {
    //scroll
    $(window).bind('scroll', function() {
        console.log('scroll');
    });
    //resize    
    $(window).bind('resize', function() {
        console.log('resize');
    });
});
